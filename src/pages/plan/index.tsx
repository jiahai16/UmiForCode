import style from './index.less'
import { IRouteProps } from 'umi'

export default function PlanPage({ children }: IRouteProps) {
  return (
    <div>
      PlanPage----
      {children}
    </div>
  )
}
